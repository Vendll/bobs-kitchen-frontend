import { Component, FunctionComponent, ReactNode } from 'react';
declare enum ORIENTATION {
    VERTICAL = 0,
    HORIZONTAL = 1
}
declare type IImage = {
    title: string;
    img: string;
    imgThumbnail?: string;
};
interface IProps {
    images: IImage[];
    reactModalStyle?: unknown;
}
interface IState {
    fistImagePosition?: ORIENTATION;
    lightBox: null | number;
}
declare class ImagesGrid extends Component<IProps, IState> {
    private _fistImage;
    private _lightBoxRef;
    constructor(props: IProps);
    private _handleImageLoad;
    private _handleImageClick;
    private _HoverMoreRender;
    _OneImageRender: FunctionComponent<{
        image: IImage;
    }>;
    _TowImageRender: FunctionComponent<{
        images: [IImage, IImage];
    }>;
    _ThreeAndMoreImageRender: FunctionComponent<{
        images: IImage[];
    }>;
    private _SelectorRender;
    render(): ReactNode;
}
export type { IImage };
export default ImagesGrid;
